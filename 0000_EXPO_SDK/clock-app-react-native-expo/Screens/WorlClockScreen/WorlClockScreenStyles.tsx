import { StyleSheet, Dimensions } from "react-native";

export const styles = StyleSheet.create({
  worldclock: {
    flex: 1,
  },
  worldclock__add: {
    position: "absolute",
    bottom: 40,
    right: 30,
  },
});
